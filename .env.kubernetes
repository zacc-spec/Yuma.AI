# Kubernetes Configuration for Yuma.AI
# Source this file to use workspace-specific kubeconfig

# Set KUBECONFIG to use this workspace's kubeconfig
export KUBECONFIG=${HOME}/Yuma.AI/.kubeconfig

# Optional: Set current context (modify if needed)
# export KUBECONTEXT=your-context-name
